# changelog — 原本 v2.0 → 改訂案 v2.1（step 5）

## 数値

| 項目 | v2.0 | v2.1 | 増減 |
|---|---:|---:|---:|
| 行数（改行の数） | 372 | 435 | **+63** |
| 文字数 | 13,003 | 16,664 | +3,661 |
| 必須項目数 | 37 | 37 | **±0** |
| KD チェックリスト error / warning 項目数 | 8 / 4 | 13 / 9 | +5 / +5 |
| TASK チェックリスト error / warning 項目数 | 8 / 3 | 14 / 8 | +6 / +5 |
| 不正パケット29件の検出数（`tools/check_packet.py` による） | 8 | 29 | +21（作り方からして高く出る。adversarial_result.md を参照） |
| 原則 P1〜P4・T1〜T4 | — | 原本と同一 | 変更なし（`tools/compare_specs.py` で、読み込んだ値が完全に一致することを確認） |

必須項目数の数え方: 1節と12節の structure で「# 必須」とコメントされた行（28）＋ 2節 block.required のキー（7）＋ source_item で「必須」と書かれた項目（2）。`tools/compare_specs.py` が数える。

## 変更一覧（改訂版に反映したもの）

| # | 変更箇所 | 変更内容 | 根拠となった所見 | 理由 |
|---|---|---|---|---|
| C01 | 1節 format_rules 3項目め | 行全体を引用符で囲んだ（文面は同じ） | CHK-01 | 引用符がないと mapping として読まれ、「: # の場合は引用符で囲む」が消えるため |
| C02 | 1節 format_rules | 「値の途中の ` #`・`: `」「YES/NO・日付・版番号は引用符」「字下げは半角スペース」「同じ階層に同じキーを2回書かない」の4項目を追加 | USE-07, ADV-12（A15） | 読み込みエラーにならずに値が消える・変わるケースを実際に再現できたため |
| C03 | 1節 format_rules 2項目め | 「英小文字」→「英小文字・数字・アンダースコア」 | CHK-06, ADV-07（A07） | 仕様自身が `read_only` などを使っていて、規則と矛盾していたため。機械的に判定できる形にした |
| C04 | 1節 structure の meta | kind に「値は KD」、kd の例を "2.1" にし「引用符で囲む」を追記 | ADV-05（A05）, USE-07 | kind の値を確かめる根拠を明文化するため。版番号が数値に変わるのを防ぐため |
| C05 | 1節 structure の handoff | 「任意（引き継ぎでは実質必須）」→「任意（別のAIや担当者に作業を引き継ぐときは書く。ないと7節で warning）」 | ADV-13（A18） | 「実質必須」は判定できないため。必須にはしていない |
| C06 | 2節 block.required.id | 「B＋3桁以上の数字」を追記 | ADV-10（A13） | 書式を確かめる基準を明文化するため |
| C07 | 2節 block.required.title | 「title に書いた要点は content にも書く」を追記 | CHK-02 | P1 で title だけを読む受け手と content を読む受け手の理解がずれないようにするため |
| C08 | 2節 block.required.why | OBSERVATION の why に書くこと（何の判断に使うか）を追記 | CHK-04 | 定義と最小例が食い違っていたため |
| C09 | 2節 optional.asserted_by | 「人の名前」→「人の氏名または役職名」 | CHK-05 | 最小例が役職名を使っていて、7節 warning の対象かどうかが決まらなかったため |
| C10 | 4節3項 | 「互いに関係のない」を「どのブロックも他方を supersedes で指していない」と定義 | ADV-09（A12） | 矛盾を判定できる形にするため |
| C11 | 5節7項 | チェック結果の書き方「チェック結果: error N件 / warning N件（warning の内容）」を定めた | USE-08 | 受け手や人間が件数を拾えるようにするため |
| C12 | 7節 | `definition`（「欠けている」＝キーがない、または値が空）を追加 | ADV-03, ADV-04（A03, A04） | 空文字や、コメントで null になった値が通っていたため |
| C13 | 7節 error | 5項目を追加（format_rules 違反 / kind・id・date の書式 / blocks の欠落 / block.id の書式 / page） | ADV-01, 02, 05, 06, 07, 10, 11, 12（A01, A02, A05, A06, A07, A13, A14, A15） | 原本の 1節・2節で必須・禁止とされていたのに、チェックリストで確かめられなかったため。必須項目は増やしていない |
| C14 | 7節 warning | 5項目を追加（DERIVED/UNKNOWN だけの OBSERVATION と CONFIRMED、置き換えの矛盾、handoff がない、仕様外のキー） | ADV-07, 08, 09, 13（A07, A08, A12, A18） | P2・5節・4節の規則に対応するチェックがなかったため。受け手に注意を促す段階なので warning にした |
| C15 | 11節の前のコメント・spec.kinds・spec.sections | 「10節は欠番」を明記。kinds を KD 0〜9節、TASK 11〜15節に修正。節番号とキー名の対応表 `spec.sections` を追加 | USE-05, USE-06 | 節番号がコメントにしかなく、読み込むと消えるため。範囲の表記が食い違っていたため |
| C16 | 5節・6節・13節・16節の節参照 | 「7節」→「7節（checklist）」のように、キー名を併記 | USE-05 | 受領パケットが節番号を取り違えた実例があるため |
| C17 | 12節 meta | kind に「値は TASK」、kd に「TASK でもキー名は kd（spec_version などの別名は使わない）」、slug に「3〜40字」を追記 | USE-04, USE-11, ADV-05 | 受領パケットが spec_version と書いた実例があるため。KD と TASK で規則を揃えるため |
| C18 | 12節 inputs・13節2項 | 任意項目 `inputs.discovery`（allowed / scope / prohibited）を追加。13節2項に「allowed が true なら scope の範囲に限って探してよい」を追記 | USE-02 | research と13節2項の矛盾を解くため。受領パケットが同じ構造を自分で足していた。任意項目で、省略すれば原本と同じ動作（探さない）になる |
| C19 | 12節 side_effect.new | 「actions の output をすべて含める（「<フォルダ>/ 配下」とまとめて書いてもよい）」を追記 | CHK-03, ADV-20（T07） | 「すべて列挙」の範囲を明確にするため。受領パケットのようにフォルダ単位で書く書き方も認めるため |
| C20 | 14節 | `definition` と error 6項目を追加（format_rules 違反 / kind・id・date の書式 / operation の値 / context / inputs・read_only / discovery の scope）。actions の項目に do と「actions がない」を追加 | ADV-14, 15, 16, 21（T01, T02, T03, T11）, USE-12 | 12節の必須項目がチェックリストで確かめられなかったため。必須項目は増やしていない（discovery の scope は、allowed: true にした場合だけ） |
| C21 | 14節 warning | 5項目を追加（existing が変更なしなのに変更を示す語がある / output が new にない / constraints の1件目 / report_back の件数 / 仕様外のキー）。主観語の例に「正確・わかりやすい・問題ない」を追加 | ADV-17, 18, 19, 20（T04, T05, T06, T07）, USE-12 | T2・T3・12節に対応するチェックがなかったため。語による判定は漏れも誤検出もありうるので warning にした |
| C22 | 9節 KD最小例 | kd を "2.1" に。B002 の content に「軸受の摩耗を候補に挙げる」を追記 | CHK-02 | title と content を揃えるため |
| C23 | 15節 TASK最小例 | kd を "2.1" に。side_effect.new に actions の output 5件をすべて列挙 | CHK-03 | 12節の「すべて列挙」に合わせるため |
| C24 | 16節・冒頭の使い方 | テンプレート `app_setup`（AIアプリの設定欄に入れる文）を追加。冒頭の使い方に1行追加 | USE-01 | 3つの用途のうち、設定ファイルとしての用途だけ書き方がなかったため |
| C25 | 冒頭コメント・spec.version | 版を 2.1 に。2.0 との関係（必須項目は増えていない、2.0 のパケットも読める）と、節番号の対応表の説明を追記 | USE-04, USE-06 | どの版の規則で読むかを受け手が判断できるようにするため |

## 提案（改訂版には反映していないもの）

| # | 提案 | 根拠となった所見 | 反映しなかった理由 |
|---|---|---|---|
| PR-01 | 既存のものを変更する依頼のために、任意の `mutation` 節（mode: create_only / modify、allowed_targets、approval_ref）を設ける。modify のときだけ、side_effect.existing に変更対象を列挙する | USE-03 | **T2（二重ロック）の意味を変えるため**（constraints の制約に従い、提案に留めた）。受領パケットで需要は観測されている |
| PR-02 | 13節1項に例外を設ける: 不備が表記ゆれだけで、依頼者に確認できる場合は、不備を記録して実行してよい | USE-10 | 受け手の判断の幅を広げる設計変更で、証拠は本タスクの1件だけのため |
| PR-03 | 6節（KD の受け手）に、error がある場合の扱い（そのブロックを判断に使わない、など）を定める | USE-09 | 挙動の設計で、どれを選ぶべきかを示す証拠がないため |
| PR-04 | 分量の目安を「8,000トークン」から「8,000字」（エディタで数えられる単位）に変える | USE-13 | 推論のみ。換算の精度も未検証のため |
| PR-05 | status の SUPERSEDED / DEPRECATED の使い方の例（旧ブロックは書き換えず、新しいブロックから supersedes で指す）を 4節に追加する | USE-14 | 推論のみ（利用者の行動は未観測）のため |
| PR-06 | 小さな作業向けに、forbidden・success_criteria の最低件数を下げた軽量版の TASK を用意する | USE-15 | 推論のみ。T1・T3 を弱めるおそれがあるため |
| PR-07 | 受け手が読む部分（1〜7節、12〜14節）だけの抜粋を別ファイルで用意する | USE-16 | 推論のみ。仕様の範囲外の追加（forbidden.soft）に当たる可能性があるため |
